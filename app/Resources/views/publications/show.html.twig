{% extends 'base_backoffice.html.twig' %}

{% block contenu %}
    <h1 class="titretable">Publication</h1>
    <div class="container">
        <table class="table table-striped table-bordered table-condensed ">
            <tbody>
                <tr>
                    <th class="thtable2 text-center">Id</th>
                    <td class="tdtable2">{{ publication.id }}</td>
                </tr>
                <tr>
                    <th class="thtable2 text-center">Doi</th>
                    <td class="tdtable2">{{ publication.doi }}</td>
                </tr>
            </tbody>
        </table>
    </div>
    <ul class="boutonCenter">
        <li class="BoutonTableShow ">
            <a href="{{ path('publications_index') }}"> <button type="button" class="btn btn-default btn-lg"><h4>Retourner à la liste</h4></button> </a>
        </li>
        <li class="BoutonTableShow ">
            <a href="{{ path('publications_edit', { 'id': publication.id }) }}"><button type="button" class="btn btn-success btn-lg"><h4>Editer</h4></button></a>
        </li>
        <li class="BoutonTableShow ">
            {{ form_start(delete_form) }}
                 <button type="submit" class="btn btn-danger btn-lg"><h4>Supprimer</h4></button>
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
