{% extends 'base_backoffice.html.twig' %}

{% block contenu %}
    <h1 class="titretable">Publication</h1>
    <div class="container">
        <table class="table table-striped table-bordered table-condensed ">
            <tbody>
                <tr>
                    <th class="thtable2 text-center">Id</th>
                    <td class="tdtable2">{{ publication.id }}</td>
                </tr>
                <tr>
                    <th class="thtable2 text-center">Doi</th>
                    <td class="tdtable2">{{ publication.doi }}</td>
                </tr>
            </tbody>
        </table>
    </div>
    <ul class="boutonCenter">
        <li class="BoutonTableShow ">
            <a class="btn btn-default btn-lg btnForm" href="{{ path('publications_index') }}">Retourner à la liste</a>
        </li>
        <li class="BoutonTableShow ">
            <a class="btn btn-success btn-lg btnForm" href="{{ path('publications_edit', { 'id': publication.id }) }}">Editer</a>
        </li>
        <li class="BoutonTableShow ">
            {{ form_start(delete_form) }}
                 <button type="submit" class="btn btn-danger btn-lg btnForm">Supprimer</button>
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
