{% extends 'base_backoffice.html.twig' %}

{% block contenu %}
    <div class="container">
        <h1 class="text-center titretable">Liste des publications</h1>
        <div class="row">
            <div class="col-lg-offset-1 col-lg-10 text-right">
                <a class="btn btn-success btn-lg btnForm" href="{{ path('publications_new') }}">Enregistrer une publication</a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-striped table-bordered table-condensed tableSorter">
                <thead>
                    <tr>
                        <th class="thtable">N°</th>
                        <th class="thtable">Doi</th>
                        <th class="thtable">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for publication in publications %}
                    <tr>
                        <td class="tdtable">{{ publication.id }}</td>
                        <td class="tdtable">{{ publication.doi }}</td>
                        <td class="tdtable puces">
                            <ul>
                                <li class="puceaenlever ">
                                    <a class="colorInherit" href="{{ path('publications_show', { 'id': publication.id }) }}"><i class="fa fa-file-text-o fa-2x " aria-hidden="true"></i></a>
                                </li>
                                <li class="puceaenlever">
                                    <a class="colorInherit" href="{{ path('publications_edit', { 'id': publication.id }) }}"><i class="fa fa-pencil fa-2x" aria-hidden="true"></i></a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
