{% extends 'base.html.twig' %}

{% block body %}
    <h1>Keywords list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Keyword</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for keyword in keywords %}
            <tr>
                <td><a href="{{ path('keywords_show', { 'id': keyword.id }) }}">{{ keyword.id }}</a></td>
                <td>{{ keyword.keyword }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('keywords_show', { 'id': keyword.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('keywords_edit', { 'id': keyword.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('keywords_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
