{% extends 'base_backoffice.html.twig' %}

{% block contenu %}
    <h1>Brevets edit</h1>


    <div class="well">
        {{ form_start(edit_form) }}
        <div class="groupequipformulaire">
            <div class="row">
                <div class="form-group">
                    {{ form_label(edit_form.brevet, "Brevet", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
                    <div class="col-sm-8 ">
                        {{ form_widget(edit_form.brevet, {'attr': {'class': 'form-control brevetAreaTaille'}}) }}
                    </div>
                </div>
            </div>
        </div>
    <ul class="boutonCenter">
        <li class="groupequipformulaire BoutonTableShow">
            <button type="submit" class="btn btn-success btn-lg"><h4>Editer</h4></button>
        </li>
        <li class="groupequipformulaire BoutonTableShow">
            <a href="{{ path('brevets_index') }}"><button type="button" class="btn btn-default btn-lg"><h4>Retourner à la liste</h4></button></a>
        </li>
        <li class="groupequipformulaire BoutonTableShow">
            {{ form_start(delete_form) }}
                <button type="submit" class="btn btn-danger btn-lg"><h4>Supprimer</h4></button>
            {{ form_end(delete_form) }}
        </li>
    </ul>
        {{ form_end(edit_form) }}
    </div>



{% endblock %}
